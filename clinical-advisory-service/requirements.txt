# clinical-advisory-service/requirements.txt
# Base image (health-ai-base) da co: faiss-cpu, numpy, scipy, neo4j,
#   google-generativeai, fastapi, uvicorn, requests, pydantic, python-dotenv
# Chi liet ke phan khac biet:

# (Khong co dependency bo sung - tat ca da nam trong base image)
# Giu file nay de pip install khong bac loi "file not found"
