# clinical-advisory-service/requirements.txt
# ⚠️  Các thư viện AI lớn (torch, faiss-cpu, sentence-transformers,
#     neo4j, numpy, fastapi, uvicorn, pydantic, requests, python-dotenv)
#     ĐÃ được cài trong health-ai-base image.
#     Chỉ liệt kê phần khác biệt so với base ở đây:

# LLM (google-gen-ai cũng đã có ở base, nhưng giữ lại để pin version rõ ràng)
google-generativeai>=0.7.0
